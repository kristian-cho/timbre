import React, { useState } from 'react';
import Grid from '@material-ui/core/Grid';

const home = () => {
    return (
        <Grid container>
            <Grid item sm={8} xs={12}>
                <p>Content</p>
            </Grid>
        </Grid>
    );
};

export default home;